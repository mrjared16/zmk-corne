name: Build ZMK firmware

# Build on push to any branch, PRs, and manual dispatch
# Key branches:
#   - claude/homerow-mod-research-*: Original mod-tap implementation
#   - claude/homerow-urob-style-*: urob's "timeless" positional homerow mods
on:
  push:
    branches:
      - '*'  # Build all branches
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
