name: Build ZMK firmware

# Build on push to any branch, PRs, and manual dispatch
# Key branches:
#   - claude/homerow-hold_tap-*: Original mod-tap implementation
#   - claude/homerow-urob-*: urob's "timeless" positional homerow mods (RECOMMENDED)
on:
  push:
    branches:
      - '*'  # Build all branches
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
